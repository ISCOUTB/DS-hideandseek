import 'package:flame/game.dart';
import 'package:flutter/material.dart';

class HideNseek extends FlameGame {
  @override
  Future<void> onLoad() async {}

  @override
  void update(double dt) {
    super.update(dt);
  }

  @override
  void render(Canvas canvas) {
    super.render(canvas);
  }
}
